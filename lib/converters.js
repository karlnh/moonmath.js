// function numToArr(num) {
// 	return Math.round(num).toString().split("").map(n => parseInt(n));
// }

// function arrToNum(arr) {
// 	return parseInt(arr.map(n => n.toString()).join(''));
// }

function addZeroes(base, arr) {	
}

module.exports = { addZeroes };